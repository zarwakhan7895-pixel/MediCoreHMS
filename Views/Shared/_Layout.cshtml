<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>MediCore City Hospital</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">MediCore Hospital</a>
            <div class="navbar-nav">
                <a class="nav-link text-white" href="@Url.Action("Index", "Dashboard")">Dashboard</a>
                <a class="nav-link text-white" href="@Url.Action("Index", "Patients")">Patients</a>
                <a class="nav-link text-white" href="@Url.Action("Index", "Doctors")">Doctors</a>
                <a class="nav-link text-white" href="@Url.Action("Index", "Admissions")">Admissions</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        @RenderBody()
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
